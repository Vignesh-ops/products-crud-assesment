# Products management crud app

# To install

npm install

# to run front end

npm run dev

# to install and run backend json server

npm install json-server

npx json-server --watch db.json --port 5000